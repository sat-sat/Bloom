section(data-ng-init="find()")
  h1 Articles
  if !user
    ul
      li(data-ng-repeat="article in articles" data-ng-show="authentication.user.username == article.creator.username")
        a(data-ng-href="/#/articles/{{article._id}}" data-ng-bind="article.title")
        br
        small(data-ng-bind="article.created | date:'medium'")
        small &nbsp;/&nbsp;
        small(data-ng-bind="article.creator.fullName")

          p(data-ng-bind="article.content")
    div(data-ng-hide="!articles || articles.length") No articles yet, why don't you <a href="/#/articles/create">create one</a>?

  else
    p Please log in to view articles
  